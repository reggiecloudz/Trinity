@model IEnumerable<ApplicationUser>

@foreach(var user in Model)
{
    <div>
        <a asp-controller="Messenging" asp-action="CreatePrivateRoom" asp-route-userId="@user.Id">
            @user.FullName
        </a>
    </div>  
}